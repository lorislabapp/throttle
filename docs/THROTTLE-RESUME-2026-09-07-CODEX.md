# Codex continuation — 2026-09-07

Complements `THROTTLE-HANDOFF-2026-09-07-PASSAGE-CLAUDE-TO-CODEX.md`.

## Active loop checkpoint — 2026-09-08 12:03 UTC

Approved G0–G9 loop remains active, worktree build/cockpit-maintenance,
branch fix/cockpit-state-observation, HEAD cc40cfb. All product edits uncommitted.
No app installation/restart, notarization or publication. Working remote agent
remains1.0.0/Node18; only the exact isolated Linux qualification was authorized/run.

Lot9: fullAppDebug606total/601PASS/5skips, VaultDebug144/Release144PASS,
lint0/baseline2983→2950, independentEND08:10:38. F007 closed locally.
Lot10: immutable transfer binding + missing mutable recovery; fresh systemd
lifecycle, trusted command descriptors and durable client requests Mac/iOS.
Node32PASS after socket fix; Shared50PASS, app38then29targetedPASS; iOSarm64
simulatorBUILD SUCCEEDED. One internal codesign failure retained, identical retry
passed. Orphan journals restore dormant tabs without processes/network. Fresh
retry/configcommand and iOSPause/Resume P2 closed statically. UI/native qualification
is still open, no global G9 claim.

Real Linux: first archive f768c4a0 failed (tmux socket too long), cleanup0, existing
service unchanged. Product moved sockets to unit RuntimeDirectory0700 under/run.
Second archive3bb05c8d77e3e5bc183aaf4aec3936bc64c54481107fd67688bdff437057a99e
PASS09:38:54→09:39:16UTC:32Node tests +3synthetic scenarios,4scope receipts empty,
final readonly confirms4unitfiles/4runtime dirs/2watchdogfiles absent and working
service active/running with same identity. Independent evidence review
END09:44:58UTC accepts this bounded claim. Timer180s after driver crash was not
triggered (run22.566s); CLI native/auth, fullHTTP, MacUI/AX/performance remain open.
Evidence audit-output/sota-20260907/lot10, exact candidate doc
THROTTLE-LINUX-QUALIFICATION-2026-09-08.md. Qualification archives/runtime/data
remain under/opt/throttle-qualification onCT134, isolated from the working agent.

Lot11 local validated: EdgeAgentService+NodeRuntime.swift prepares private official
Node24.20.0 binary (x64+arm64 SHA pinned); Installation/Deployment use it and
unitText ExecStart targets it. package engines22/24LTS. Not deployed. Shared
tests52PASS then2 targetedPASS after helper extraction, exit0; generated shell syntax
and checksum-failure preservation covered, lint0, independent END10:06:43 no new
P0/P1/P2. No successful installer or arm64Linux proof. Evidence lot11.
Next: native/HTTP/MacUI/AX/memory/VaultXPC gates, Release/CI and exact publication.
Lot12 host isolation fixed and independently reviewed END10:25:03. All testhosts
use memoryDB/no license, no production startup/updater/menu/callbacks; UI previews
have inert dependencies. Lint0 baseline2947. Full607XCTest/5skip/0fail passed but
SwiftTesting fixture crashed: whole runFAIL. Default in-memory spaces corrected,
2XCTest+3SwiftTesting targetedPASS/exit0; independent END10:32:53. No fullsuitePASS
post-fix yet. Evidence lot12, all failures retained.
Distinct ad hoc UI candidate lot12/ui-candidate/ThrottleQualification.app, bundleID
com.lorislab.throttle.qualification.lot12. Direct node spawnSIGABRT beforeAppDelegate;
LaunchServices in sandbox -10827; exact open escalated approvedPASS/PID51479.
Real FR capture940x652 + AX inlot12, workingGUI singletonPID57439 unchanged.
Lot12 and lot13 preview processes now closed, confirmed via list_apps. Lot13
corrected approval-step state, AX containment and 42 French strings; activation
notification refreshes real service status and health after approval, inert hosts
return before SMService reads. Six targeted tests PASS/exit0; lint0. Independent
activation-return review END10:47:12UTC, no new P0/P1/P2.
Real lot13 FR disabled/requiresApproval captures and AX retained under lot13,
exact ad hoc candidate manifest and execution events. WorkingGUI singleton57439
unchanged. Independent render review END10:55:50 closes the initial state/AX/FR
findings within these two inert previews, finds two truncations at940px. Source
now wraps sidebar button and puts status on its own growing line; new capture
pending. Blue button after Tab does not establish AX focus or keyboard/VoiceOver
qualification. No real macOS service approval or XPC connection exercised.
Lot13 disposition revalidated in a distinct ad hoc Release copy
com.lorislab.throttle.qualification.lot13layout, manifest/captures under
lot13/layout-candidate. Both truncations resolved940x652, independentEND11:13:42.
Copy closed, list_apps confirms absent. No AX-focus/VoiceOver/actual approval proof.
Lot14: real read-only native observation showed Codex0.147.0 keeps parent+subagent
rollout writers in the same PID55582/cwd. Filter source/thread_source rejects
non-user origins before unique writableFD selection, preserves ambiguous-root
refusal. Official rust-v0.147.0 protocol verified. Nine native tests pass, together
27XCTest+3SwiftTestingPASS exit0 for native/return/journal/Vault. Independent
END11:04:07 no P0/P1/P2. Evidence lot14.
App Release arm64 build and stable incremental pass both exit0, bundle structure
PASS unsigned, but these bytes predate lot15. No distribution signature/install.
Critical lint correction: earlier naked swiftlint commands (lots9,12,13) used
Homebrew0.65.1, which ignores29literal URLunwraps; CI pins0.63.2. Independent exact
reproduction END11:09:29:0.63.2=29FAIL,0.65.1=0. Earlier lint0 does not prove pinned
gate, other tests/builds unaffected. Lot15 fixes29URLunwraps via guarded endpoints,
optional model links, XCTestunwrap fixtures and bounded helper extraction.
Pinned0.63.2 fullstrict no-cache nowPASSexit0. Baseline2947→2886:4existingdimension
reductions,61stale/resolvedentriesremoved,0newexceptions, proof lot15/baseline-delta.
New scripts/verify-swiftlint.sh pins version and is used by CI and build-dmg;
defaultPATH0.65.1 rejectedexit78, THROTTLE_SWIFTLINT_BIN=/private/tmp/throttle-swiftlint-0.63.2/swiftlint passesexit0.
FullDebug initial lot15 compileFAIL (EmbeddedModelRuntimeTests still assumed
nonoptionalURL), fixed test withXCTUnwrap. Recovery fullsuite session62267 completedexit0:610XCTest/5skip/0fail plus3SwiftTesting,
608PASStotal. Independentlot15END11:25:05 confirms23stablefiles/no newP0/P1/P2,
ratchet reproduced fromoutsidecheckout. Evidence lot15/final-validation.json.
Lot16 MCPDebug rebuilt withnativeSwiftPMexit0, smokeexit0:65documents/738chunks,
schema7/SQLCipherintegrityPASS. Localread-onlyDeepSearsh and ephemeralDB/key, noXPC.
Real13questionsmeasured:11in-grant,2out-of-grantonly; Recall@5chunks0.7273,
MRR0.5606,nDCG0.5799,citationhash/scopePASS. Historical labels not refreshed; noP95,
receipt-leg or nativeXPC proof. IndependentEND11:50:52 revalidated scores/hash/scope; no newP0/P1. Evidence lot16.
Knownrealquestiondataset:13Throttlequestions,11with at leastone relevantdoc inside
Throttlegrant;2questions have onlyout-of-scope references. Keep coverage separate
from retrieval and do not treat old title-derived benchmark as productquality.
WorkingGUI singleton lastverified57439; all preview copies stopped.
Node_repl sky bootstrapped; never call get_app_state on a stopped candidate, because
implicit launch would omit test flags. Launch exact args with approved open first;
verify running via list_apps, then Computer Use only for UI interactions.

Lot17 release preparation: project.yml3.6.0/219, notes docs/releases/3.6.0-notes.html.
Local Developer ID universal archive/export/DMG prepare-only completedexit0,
session43473 closed. Strict app/widget/Vault/Sparkle/bundle/mountedDMG checksPASS.
Candidate executable9bdc4cb4da9ed7bbb89bd59e044397a498c9a8f1a56df9351f8fdf84b0638fd7;
DMG32284439bytes SHAe30c2ef0366d0de9c1ded3e50dcbf3bcb84382b49e8b589e5c83b99b4b727c5f.
Manifest106appentries, dSYMs and prepare-build.log retained under
 audit-output/sota-20260907/release-3.6.0-219. No notarization/install/upload.
Gitleaks214currenttextfiles: exit1/one generic-api-key alert, triaged as existing
Sparkle PUBLIC key matchingHEAD and installedapp;0confirmedsecrets, noallowlist.
Source snapshot0afe40364f3a40b9abc8ad73595b1536218476d203f727122b1bf9551be57586.
Fresh runtimepreflight: seven direct live shells ofGUI57439;147non-zombieprocesses
includingapp,54zombies;50persistedtabs/39hibernated/49nativeIDs/0remote. Current
loopPID55582 isTerminal-owned. Vault31543stillholdsDB/WAL. No backup/stop done.
Concrete qualification/rollback plan prepared in ignored release folder; fresh
explicit installation/restart/service approval still required. Public repo confirmed
lorislabapp/throttle, defaultmain. Source checkpoint/CI approval not yet requested.

Disk fluctuates to zero with concurrent unrelated work. Only our432MiB completed
VaultRelease cache was deleted with explicit tool approval after lsof showed no
open files;144PASS logs/sources preserved. No other cleanup performed. First
archive ENOSPC failure retained; both executed archives were rebuilt/hashed exactly.
G7 nowPARTIAL, loop remains PRODUCTBLOCKED overall. Read current remediation
journal before continuing; sections below are historical checkpoints only.

## Reconciled checkpoint

- Local `main`, `release/3.5.2-218`, and their origin tracking refs were at
  `cc40cfb` when this continuation started. No remote fetch/publication verification
  was performed in this continuation.
- The user's original checkout remains on `feat/research-vault-lots-1-4`,
  with its untracked `.install-backups/` preserved.
- Isolated worktree: `build/cockpit-maintenance`, branch
  `fix/cockpit-state-observation`, based on `cc40cfb`.
- Installed `/Applications/Throttle.app` reports **3.5.0 (216)**. The earlier
  release ledger's references to an installed 3.5.1 do not match this fresh read.
- Free disk space at the start: 7.7 GiB. Build caches consume additional space.

## First bounded fix

Source inspection found that the state dot's isolation was bypassed in all three
layouts (tabs, rail, mission): `stateDot(tab).help(stateDotHelp(tab))` evaluated
`tab.state` in the root's observation scope. For a spawned, unpaused, unrestricted
session not waiting for input, `state` reads `lastActivityAt`, which terminal output
updates. This creates a path for output to invalidate the whole cockpit despite
the dedicated dot view. No new memory-footprint measurement is claimed.

`SessionStateDot.swift` now owns both the shape and tooltip, and all three call
sites construct the dot without reading state. Tooltip strings and state decisions
are preserved. The activity filter, its 60-second window, cached counts, and the
model's invalidation rules are unchanged.

Two observation tests cover construction without parent subscription and body
subscription to state changes, using dormant-to-hibernated transitions without
starting a terminal process.

## Validation

- SwiftLint **0.63.2**, full strict lint without cache: PASS.
- Baseline regenerated by that binary from this worktree under the user's home:
  3,063 -> 3,061 entries, zero absolute paths, no added file/rule exceptions.
- `git diff --check`: PASS.
- Focused Xcode tests: **PASS, 12 tests, zero failures**, at 11:20 CEST:
  `ActivityFilterTests` (10) and `SessionStateDotTests` (2). Local Debug arm64
  build/test used Xcode-beta's macOS 27 SDK, not the CI's stable Xcode lane.
- The initial sandbox attempt could not write Swift package/compiler caches;
  the identical command succeeded outside the sandbox.
- Durable local evidence inside this worktree:
  `audit-output/cockpit-maintenance-20260907/FocusedTests.xcresult` and
  `audit-output/cockpit-maintenance-20260907/test-summary.json` (git-ignored).
  Full build log: `/private/tmp/throttle-cockpit-maintenance-test.log`.
- Test DerivedData is retained at `/private/tmp/throttle-cockpit-maintenance-derived`
  for incremental continuation. Free disk after testing: 3.9 GiB.

Reproduction:

```sh
xcodebuild test -project Throttle.xcodeproj -scheme Throttle \
  -configuration Debug -destination 'platform=macOS' \
  -derivedDataPath /private/tmp/throttle-cockpit-maintenance-derived \
  -skipPackagePluginValidation -skipMacroValidation \
  -only-testing:ThrottleTests/ActivityFilterTests \
  -only-testing:ThrottleTests/SessionStateDotTests -parallel-testing-enabled NO
```

## Still open

- The complete decomposition of `MultiCockpitRoot.swift` and
  `MultiCockpitModel.swift` remains open; this first extraction fixes an actual
  observation boundary rather than merely moving methods into extensions.
- Visual/accessibility validation of Plan and AI Routing, a future release of
  unpublished fixes, and installation/relaunch require their own next steps.
- No install, production relaunch, version bump, upload, publication, commit,
  push, website edit, or Claude configuration edit was performed.


## Linux execution checkpoint — 09:25 UTC

The exact isolated qualification archive f768c4a06e8690f718098f6fafeeb83cf3c5d8d1583490e159b0f4c9248d42a6
was approved through command escalation and transferred to CT134 via pve. Remote
archive SHA matched. Qualification is running, no verdict yet. Candidate path
/opt/throttle-qualification/candidate-f768c4a06e8690f7 ; local execution log under
lot10/linux-qualification-execution.log. Runtime is private Node24, not a service
upgrade. Inspect its report and scoped cleanup before any next mutation.

Disk ENOSPC interrupted the first local archive. The sole432MiB completed Vault
Release cache was removed with explicit tool approval after lsof showed no open
files; sources and144PASS test logs preserved. No other cache/worktree/app removed.
Current prepared archive was rebuilt and fully hashed after cleanup.


Linux result 09:25:33UTC:31Node tests PASS on privateNode24.20.0; systemd harness
FAIL on first freshStart, no successful runtime scenario. Rootrun5a7caf78-900b-4607-b571-b574086283cd.
First unit throttle-session-8948ce97-feda-4abe-85b5-f6a9ad154ad6 failed during ExecStart.
Cleanup returned populated0, inactive/failed, productionServiceUnchanged=true.
Need exact journalctl for this unit before correction. The candidate remains on CT134,
working service/Node not upgraded. External qualification is still not PASS.


Linux retry checkpoint09:32UTC: cause firstFAIL confirmed by journalctl
and archived linux-first-helper-journal.log: tmux socket path too long.
Product socket changed to bounded /run/<unitbasename>/terminal.sock, with
systemd RuntimeDirectory0700; shared by launch/confirm/attach, journal unchanged.
32NodePASS local, independent END09:29:42UTC static GO. Second exact archive
3bb05c8d77e3e5bc183aaf4aec3936bc64c54481107fd67688bdff437057a99e was approved
through command escalation; execution RUNNING at this checkpoint, no PASS yet.
Remote /opt/throttle-qualification/candidate-3bb05c8d77e3e5bc ; local log
lot10/linux-socket-qualification-execution.log. Observe result and scoped cleanup.
